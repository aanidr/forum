%h1= "Hi #{@user.name}!"
= image_tag @user.avatar

= render 'application/menu'

.text Here is the list of your topics:
#topics= render 'topics/topics', topics: @user.topics	

.link=	link_to 'Edit account', edit_user_path(@user), class: 'link'
.link= link_to 'Delete account', user_path(@user), method: :delete
